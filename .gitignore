__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.DS_Store

# Receipts, transcriptions, catalog sheets and caches stay private: everything under data/.
# The two contract snapshots the demo reads (web/purchases.json, web/insights.json) are
# published deliberately: they are the item-level history the insights are computed from.
data/
